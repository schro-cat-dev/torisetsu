interface:
  display_name: "Config Driven Harness Tooling"
  short_description: "configで差し替える汎用ハーネスツール設計と実装"
  default_prompt: "Use $config-driven-harness-tooling to design or build a reusable harness tool whose concrete rules come from config files."
policy:
  allow_implicit_invocation: true
