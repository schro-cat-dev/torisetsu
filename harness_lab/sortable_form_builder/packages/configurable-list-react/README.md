# Configurable List React

設定駆動フォーム、dnd-kit一覧、行内詳細、構成編集、モーダルのReact UIです。

```tsx
import { ConfigurableCollectionEditor } from "@torisetsu/configurable-list-react";
import "@torisetsu/configurable-list-react/styles.css";
```
