# artifact-version-contract

## 目的

成果物の現在バージョンを、実装、package、台帳で一致させる。

## 完了条件

- `package.json` の `version` が現在版と一致している。
- `package-lock.json` の root version が現在版と一致している。
- `docs/artifact-version-ledger.md` に現在版の章がある。
