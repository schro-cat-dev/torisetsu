# User Improvement Rules

ユーザー自身の改善につながる気づきを、短く保存する場所。

## 記録ルール

- 1件は短くまとめる。
- 書く項目: `日付`、`分かったこと`、`状況`、`理由`、`次の動き`、`例`。
- 数字が役立つ場合は、`優先度` と `おすすめ度` を使う。`5` が高く、`1` が低い。
- 会話全文は入れない。詳しい流れは `internal_refs/chat_logs/` を見る。
- 断定や診断ではなく、作業を進めやすくするための仮説として扱う。
